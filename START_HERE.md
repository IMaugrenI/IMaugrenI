# Start here

The broad public entry point for this repository family is `tof-showcase`.

This profile lists several repository types:
- public architectural frame
- runnable local product repos
- reduced frame repos
- method repos

Do not read them as one flat set.
Start with `tof-showcase`, then move to the narrower repository you actually need.
